# 2026-02-27 记忆笔记

## 子智能体修复 & Dashboard 重建

### 子智能体 skill 配置修复
- 问题：子智能体无法使用 workspace/skills 下的 skill
- 原因：之前用错误的 `skillPaths` 配置方式
- 修复：通过 `skills.load.extraDirs` 指向主 workspace 的 skills 目录（官方文档正确做法）

### 子智能体模型兼容性
- **qwen3-coder-plus**：✅ 唯一完全可靠的子智能体模型
- **gpt-5.3-codex**：小任务 OK，大任务超时（openai-responses API）
- **gemini-3-1-pro**：本地代理不稳定，大 prompt 返回空，小任务文件可能消失
- **kimi-k2.5**：直接 failed，原因未查明
- 可疑因素：agentDir 里的 auth.json `default_profile` 可能干扰 auth 解析
- **解决方案**：用 `sessions_spawn` 的 `model` 参数直接 override 为 qwen3-coder-plus

### Dashboard 重建完成
- server.js：9 个 API + Skills API（后端 agent 补充）
- index.html：深色主题，Vercel 风格，卡片式布局
- 启动方式：`node /Users/tingjing/.openclaw/workspace/projects/dashboard/server.js`
- 端口：8899

### 配置变更
- thinkingDefault: medium → high
- skills.load.extraDirs 添加
- 动态心跳逻辑写入 HEARTBEAT.md
- 前端 agent 模型：kimi-k2.5（但实际用 qwen3-coder-plus override）
- 后端 agent 模型：codex（但实际用 qwen3-coder-plus override）

### 待办
- 排查 kimi-k2.5、gemini、codex 在 sub-agent 模式下的兼容性问题
- 清理 agentDir auth.json 的 default_profile 设置
- Dashboard 前端还可以进一步美化

---

## 多智能体协同优化（上午）

### 完成的工作

1. **Agent 模型配置更新**
   - frontend-engineer: gemini-3-1-pro（从 kimi 改）
   - math-teacher: kimi-k2.5（从 opus 改）
   - backend-engineer: gpt-5.3-codex（保持）

2. **降级策略固化**
   - 写入 MEMORY.md 和 docs/multi-agent-optimization.md
   - 创建 scripts/spawnWithFallback.js 工具

3. **Dashboard 模型统计 API**
   - GET /api/model-stats 返回所有 agent 的模型配置和降级链

4. **Cron 任务下沉改造**
   - 3 个 cron 任务 payload 简化为触发信号
   - SOUL.md 加入 cron 分发规则：收到触发 → 立即 spawn 给 ops-agent

5. **文档整理**
   - 完整的 OpenClaw 命令列表（包括 sessions、gateway、cron、memory 等）

### 新增文件
- `/Users/tingjing/.openclaw/workspace/docs/multi-agent-optimization.md`
- `/Users/tingjing/.openclaw/workspace/scripts/spawnWithFallback.js`

### 下一步
- 监控 cron 任务是否按新流程执行
- 验证降级策略实际效果

## 历史记录（workspaceq 误删事件）
- 已在之前的会话中处理和恢复
- workspaceq 目录已确认不存在
- 所有关键文件已恢复

## 个人账户记录（晚间）
- 用户已在 https://hdfans.org/ 注册 PT 账号
- 备注：这是用户要求长期记住的站点账户信息（仅内部记忆，不外发）
